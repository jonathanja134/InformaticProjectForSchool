{
  "dependencies": {
    "ethers": "^6.8.0",
    "fs-extra": "^11.1.1",
    "solc": "0.8.7-fixed"
  },
  "scripts": {
    "compile": "yarn solcjs --bin --abi --include-path node-modules/ --base-path . -o . wallet.sol"
  }
}
